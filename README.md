The case_details csv file is the dataset
I used the data Load script to upload the tables into sql
Then using the search 1 and search 2 scripts I found non-standard values int he normality column and inputed the correct values
Using the contienent creation script I created the contienent column
To get the Korean data subset I used the Korean data script
